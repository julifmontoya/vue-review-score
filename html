<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style type="text/css">
    /* ==========================================================================
     Review scores__wrapper 
   ========================================================================== */
    .score-bar__title {
        display: inline-block;
        vertical-align: top;
        width: 100%;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .score-bar__score {
        vertical-align: top;
        width: 30px;
        text-align: right;
        float: right;
        font-size: 12px;
        line-height: 18px;
        font-weight: 500;
    }

    .score-bar__bar {
        vertical-align: top;
        height: 10px;
        background-color: #ededed;
        position: relative;
        overflow: hidden;
        margin-top: 3px;
        border-radius: 2px;
    }

    .rating__value {
        width: auto;
        height: 100%;
        display: block;
        background-color: #fccc4d;
        border-radius: 4px;
    }

    .score-bar__bar {
        background-color: #dfe0e2;
    }

    .score-bar__score {
        color: inherit;
    }
</style>

<body>
    <div>
        <ul class="score__card">
            <li class="score__item" v-for="(score, index) in scores" :key="index">
                <div class="score-bar">
                    <span class="score-bar__title">{{ score.title }}</span>
                    <span class="score-bar__score">{{ score.percent }}%</span>
                    <div class="score-bar__bar">
                        <span class="score-bar__value" :data-value="score.percent"
                            :style="'width:' + score.percent + '%'"></span>
                    </div>
                </div>
            </li>
        </ul>
</body>

</html>
